name: Get Citation Data

on: 
  page_build: 
  schedule:
  - cron:  '0 6 * * 1'
  workflow_dispatch:

permissions:
  contents: write 


jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install Reqs
      run: |
        sudo apt-get install python3-setuptools
    - name: Run
      run: |
        cd ./google_scholar_crawler
        pip3 install -r requirements.txt
        python3 main.py
        git add "../assets/results/"
        git config --global user.email "dwaynepan5277@gmail.com"
        git config --global user.name "yPanStupidog"
        git commit -m "* update `date '+%Y-%m-%d %H:%M:%S'`"
        git push
